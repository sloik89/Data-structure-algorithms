const { maxNumberArray, maxNumberForLoop } = require("./max-number");
const result = maxNumberArray([99, 88, 10, 9, 1, 2, 3, 200]);
const resultLoop = maxNumberForLoop([0, 1, 5, 33, 55, 100, 500]);
console.log(resultLoop);
